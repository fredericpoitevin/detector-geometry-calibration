

class Distance:
    def __init__(self):
        pass 

    @staticmethod
    def optimize_index():
        ## 1. accept a group of cxi files, with the same geom file
        ## 2. select high likelihood images.  
        ## 3. submit indexing of high likelihood images
            ## 3.1 Index some: 
                ## update center&unitcell first until the center doesn't change
            ## 3.2 Index none:
                ## select very top 10 images
                ## index with scan of center/distance
                ## if none of them can be indexed, QUIT
        
        

        return 